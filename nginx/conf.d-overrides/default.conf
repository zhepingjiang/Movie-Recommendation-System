# Intentionally empty. Shadows /etc/nginx/conf.d/default.conf that ships in the base
# nginx image (the stock "Welcome to nginx" page), which would otherwise win for any
# request whose Host header doesn't match ${DOMAIN} exactly (e.g. hits by raw IP) since
# our real server blocks are declared default_server in app.conf.template.
